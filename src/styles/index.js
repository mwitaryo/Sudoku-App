import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  table: {
    flex: 1,
    marginBottom: 30
  },
  boardContainer: {
    flexDirection: 'row'
  },
  messageContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 1,
    padding: 8
  }
});
